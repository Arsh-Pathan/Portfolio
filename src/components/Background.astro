---
---

<canvas id="background"></canvas>

<script>
  import * as THREE from "three";

  const canvas = document.getElementById("background") as HTMLCanvasElement;

  if (canvas) {
    const scene = new THREE.Scene();
    // Thicker fog to hide the end of the tunnel
    scene.fog = new THREE.FogExp2(0x000000, 0.0008);

    const camera = new THREE.PerspectiveCamera(
      75,
      window.innerWidth / window.innerHeight,
      1,
      8000
    );
    // Camera will travel from Z=1000 down to Z=-5000 based on scroll

    const renderer = new THREE.WebGLRenderer({
      canvas,
      alpha: true,
      antialias: true,
    });

    renderer.setSize(window.innerWidth, window.innerHeight);
    renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));

    // ----------------------------------------------------
    // 1. NEURAL NETWORK TUNNEL
    // ----------------------------------------------------
    const particleCount = 700; 
    const maxDistance = 250;
    const maxDistanceSq = maxDistance * maxDistance;

    const particles = new THREE.BufferGeometry();
    const particlePositions = new Float32Array(particleCount * 3);
    const particleVelocities = [];

    // Distribute particles in a massive long corridor
    const tunnelWidth = 2000;
    const tunnelHeight = 2000;
    const tunnelLength = 7000; // From Z=1500 to Z=-5500

    for (let i = 0; i < particleCount; i++) {
      particlePositions[i * 3] = (Math.random() - 0.5) * tunnelWidth;
      particlePositions[i * 3 + 1] = (Math.random() - 0.5) * tunnelHeight;
      // Z goes from 1500 down to -5500
      particlePositions[i * 3 + 2] = 1500 - Math.random() * tunnelLength;

      particleVelocities.push({
        x: (Math.random() - 0.5) * 0.8,
        y: (Math.random() - 0.5) * 0.8,
        z: (Math.random() - 0.5) * 0.8,
      });
    }

    particles.setAttribute("position", new THREE.BufferAttribute(particlePositions, 3));

    const pMaterial = new THREE.PointsMaterial({
      color: 0xffffff,
      size: 3,
      transparent: true,
      opacity: 0.6,
      blending: THREE.AdditiveBlending,
    });

    const particleSystem = new THREE.Points(particles, pMaterial);
    scene.add(particleSystem);

    // Lines for Neural Connections
    const maxLines = 15000;
    const linePositions = new Float32Array(maxLines * 6);
    const lineColors = new Float32Array(maxLines * 6);

    const linesGeometry = new THREE.BufferGeometry();
    linesGeometry.setAttribute("position", new THREE.BufferAttribute(linePositions, 3));
    linesGeometry.setAttribute("color", new THREE.BufferAttribute(lineColors, 3));
    linesGeometry.setDrawRange(0, 0);

    const lineMaterial = new THREE.LineBasicMaterial({
      vertexColors: true,
      transparent: true,
      opacity: 0.35,
      blending: THREE.AdditiveBlending,
      depthWrite: false
    });

    const linesMesh = new THREE.LineSegments(linesGeometry, lineMaterial);
    scene.add(linesMesh);

    // ----------------------------------------------------
    // 2. NANOPARTICLE WORM
    // ----------------------------------------------------
    const wormLength = 200;
    const wormGeometry = new THREE.BufferGeometry();
    const wormPositions = new Float32Array(wormLength * 3);
    
    // Initialize worm out of view
    for(let i=0; i<wormLength * 3; i++) {
        wormPositions[i] = 0;
    }
    
    wormGeometry.setAttribute('position', new THREE.BufferAttribute(wormPositions, 3));
    
    // Make the worm glow brightly
    const wormMaterial = new THREE.PointsMaterial({
      color: 0xffffff,
      size: 6,
      transparent: true,
      opacity: 0.9,
      blending: THREE.AdditiveBlending
    });
    
    const worm = new THREE.Points(wormGeometry, wormMaterial);
    scene.add(worm);

    // ----------------------------------------------------
    // INTERACTIVITY & ANIMATION
    // ----------------------------------------------------
    let mouseX = 0;
    let mouseY = 0;
    let scrollPercent = 0;
    
    // Calculate scroll percentage mapping to tunnel length
    const calculateScroll = () => {
        const h = document.documentElement;
        const b = document.body;
        const st = 'scrollTop';
        const sh = 'scrollHeight';
        
        const scrollAmount = (h[st] || b[st]);
        const maxScroll = (h[sh] || b[sh]) - h.clientHeight;
        
        scrollPercent = maxScroll > 0 ? scrollAmount / maxScroll : 0;
    };

    document.addEventListener("scroll", calculateScroll);
    window.addEventListener("resize", calculateScroll);

    document.addEventListener("mousemove", (event) => {
      mouseX = (event.clientX - window.innerWidth / 2) * 0.3;
      mouseY = (event.clientY - window.innerHeight / 2) * 0.3;
    });

    let time = 0;

    function animate() {
      requestAnimationFrame(animate);
      time += 0.015;

      // 1. Update Network Particles
      const positions = particleSystem.geometry.attributes.position.array as Float32Array;
      
      for (let i = 0; i < particleCount; i++) {
        positions[i * 3] += particleVelocities[i].x;
        positions[i * 3 + 1] += particleVelocities[i].y;
        positions[i * 3 + 2] += particleVelocities[i].z;

        // Bounce inside tunnel width/height, loop Z
        if (Math.abs(positions[i * 3]) > tunnelWidth / 2) particleVelocities[i].x *= -1;
        if (Math.abs(positions[i * 3 + 1]) > tunnelHeight / 2) particleVelocities[i].y *= -1;
        
        // If particle goes behind camera, loop it to the deep end of the tunnel
        if (positions[i * 3 + 2] > 1500) positions[i * 3 + 2] = -5500;
        if (positions[i * 3 + 2] < -5500) positions[i * 3 + 2] = 1500;
      }
      particleSystem.geometry.attributes.position.needsUpdate = true;

      // 2. Rebuild Neural Connections
      let vertexCount = 0;
      let colorCount = 0;

      for (let i = 0; i < particleCount; i++) {
        for (let j = i + 1; j < particleCount; j++) {
          const dx = positions[i * 3] - positions[j * 3];
          const dy = positions[i * 3 + 1] - positions[j * 3 + 1];
          const dz = positions[i * 3 + 2] - positions[j * 3 + 2];
          const distSq = dx * dx + dy * dy + dz * dz;

          if (distSq < maxDistanceSq) {
            linePositions[vertexCount++] = positions[i * 3];
            linePositions[vertexCount++] = positions[i * 3 + 1];
            linePositions[vertexCount++] = positions[i * 3 + 2];

            linePositions[vertexCount++] = positions[j * 3];
            linePositions[vertexCount++] = positions[j * 3 + 1];
            linePositions[vertexCount++] = positions[j * 3 + 2];

            const alpha = 1.0 - Math.sqrt(distSq) / maxDistance;
            const intensity = alpha * 0.7; 

            lineColors[colorCount++] = intensity;
            lineColors[colorCount++] = intensity;
            lineColors[colorCount++] = intensity;

            lineColors[colorCount++] = intensity;
            lineColors[colorCount++] = intensity;
            lineColors[colorCount++] = intensity;

            if (vertexCount >= maxLines * 6) break;
          }
        }
        if (vertexCount >= maxLines * 6) break;
      }

      linesMesh.geometry.setDrawRange(0, vertexCount / 3);
      linesMesh.geometry.attributes.position.needsUpdate = true;
      linesMesh.geometry.attributes.color.needsUpdate = true;
      
      // 3. Update Nanoparticle Worm
      const wPositions = worm.geometry.attributes.position.array as Float32Array;
      
      // Shift particles back to create trail
      for(let i = wormLength - 1; i > 0; i--) {
        wPositions[i*3] = wPositions[(i-1)*3];
        wPositions[i*3+1] = wPositions[(i-1)*3+1];
        wPositions[i*3+2] = wPositions[(i-1)*3+2];
      }
      
      // Calculate complex 3D Lissajous curve for worm head
      // The worm always stays somewhat near the camera's current Z depth
      const currentCameraZ = camera.position.z;
      
      wPositions[0] = Math.sin(time * 1.2) * 500 + Math.cos(time * 0.8) * 200;
      wPositions[1] = Math.cos(time * 1.5) * 400 + Math.sin(time * 0.5) * 300;
      // Swims around the camera depth, sometimes in front, sometimes behind
      wPositions[2] = currentCameraZ - 300 - Math.sin(time) * 800;
      
      worm.geometry.attributes.position.needsUpdate = true;

      // 4. Update Camera (Scroll mapping)
      // Map scroll from Z=1000 (top of page) to Z=-4500 (bottom of page)
      const startZ = 1000;
      const endZ = -4500;
      const targetZ = startZ + (endZ - startZ) * scrollPercent;
      
      // Interpolate camera Z for smooth scroll tracking
      camera.position.z += (targetZ - camera.position.z) * 0.08;

      // Mouse parallax
      camera.position.x += (mouseX - camera.position.x) * 0.05;
      camera.position.y += (-mouseY - camera.position.y) * 0.05;
      
      camera.lookAt(scene.position.x, scene.position.y, targetZ - 1000); // Always look down the tunnel

      renderer.render(scene, camera);
    }

    window.addEventListener("resize", () => {
      camera.aspect = window.innerWidth / window.innerHeight;
      camera.updateProjectionMatrix();
      renderer.setSize(window.innerWidth, window.innerHeight);
    });
    
    // Trigger initial calculations
    calculateScroll();
    animate();
  }
</script>

<style>
  #background {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -1;
    background: transparent;
    pointer-events: none;
  }
</style>
