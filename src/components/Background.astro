---
---

<canvas id="background"></canvas>

<script>
  import * as THREE from "three";

  const canvas = document.getElementById("background") as HTMLCanvasElement;

  if (canvas) {
    const scene = new THREE.Scene();
    // Removed Fog to keep pure black/white visibility throughout the journey

    const camera = new THREE.PerspectiveCamera(
      75,
      window.innerWidth / window.innerHeight,
      1,
      8000
    );

    const renderer = new THREE.WebGLRenderer({
      canvas,
      alpha: true,
      antialias: true,
    });
    renderer.setSize(window.innerWidth, window.innerHeight);
    renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));

    // ----------------------------------------------------
    // TEXTURE GENERATORS (No more square boxes)
    // ----------------------------------------------------
    const createCircleTexture = (glow = false) => {
       const texCanvas = document.createElement('canvas');
       texCanvas.width = 64;
       texCanvas.height = 64;
       const ctx = texCanvas.getContext('2d');
       if(glow) {
           const gradient = ctx!.createRadialGradient(32, 32, 0, 32, 32, 32);
           gradient.addColorStop(0, 'rgba(255, 255, 255, 1)');
           gradient.addColorStop(1, 'rgba(255, 255, 255, 0)');
           ctx!.fillStyle = gradient;
           ctx!.fillRect(0, 0, 64, 64);
       } else {
           ctx!.beginPath();
           ctx!.arc(32, 32, 30, 0, 2 * Math.PI);
           ctx!.fillStyle = 'white';
           ctx!.fill();
       }
       return new THREE.CanvasTexture(texCanvas);
    };
    
    const createMathTexture = (text: string) => {
       const texCanvas = document.createElement('canvas');
       texCanvas.width = 128;
       texCanvas.height = 128;
       const ctx = texCanvas.getContext('2d');
       ctx!.fillStyle = 'rgba(255, 255, 255, 0)';
       ctx!.fillRect(0, 0, 128, 128);
       ctx!.font = 'bold 80px serif';
       ctx!.fillStyle = 'rgba(255, 255, 255, 0.8)';
       ctx!.textAlign = 'center';
       ctx!.textBaseline = 'middle';
       ctx!.fillText(text, 64, 64);
       return new THREE.CanvasTexture(texCanvas);
    };
    
    const circleTex = createCircleTexture(false);
    const glowTex = createCircleTexture(true);

    // ----------------------------------------------------
    // PATH HELPER
    // ----------------------------------------------------
    function getPathPoint(progress: number) {
       const z = 1200 - progress * 5700;
       const rads = progress * Math.PI * 2;
       const x = Math.sin(rads * 1.5) * 1000;
       const y = Math.cos(rads * 1.2) * 800;
       return new THREE.Vector3(x, y, z);
    }

    // Array to hold all major scene objects for distance fading
    const sceneElements: any[] = [];
    const updatables: any[] = []; // for animations

    // ----------------------------------------------------
    // ZONE 1: FLOATING STRUCTURED NEURAL NETWORKS
    // ----------------------------------------------------
    function createStructuredNN(progress: number, offset: THREE.Vector3) {
       const center = getPathPoint(progress).add(offset);
       const group = new THREE.Group();
       
       const layers = [3, 5, 5, 2]; // Input, Hidden 1, Hidden 2, Output
       const nodeGeo = new THREE.BufferGeometry();
       let nodeCount = 0;
       layers.forEach(l => nodeCount += l);
       
       const pos = new Float32Array(nodeCount * 3);
       const layerPositions: THREE.Vector3[][] = [];
       
       let idx = 0;
       const xSpacing = 60;
       const ySpacing = 30;
       
       for(let i=0; i<layers.length; i++) {
           const nodes = layers[i];
           const x = (i - layers.length/2) * xSpacing;
           const currentLayer: THREE.Vector3[] = [];
           
           for(let j=0; j<nodes; j++) {
               const y = (j - nodes/2) * ySpacing;
               pos[idx*3] = x;
               pos[idx*3+1] = y;
               pos[idx*3+2] = 0;
               currentLayer.push(new THREE.Vector3(x, y, 0));
               idx++;
           }
           layerPositions.push(currentLayer);
       }
       
       nodeGeo.setAttribute('position', new THREE.BufferAttribute(pos, 3));
       const nodeMat = new THREE.PointsMaterial({ 
           map: circleTex, size: 8, color: 0xffffff, transparent: true, opacity: 0.8, depthWrite: false 
       });
       const nodesMesh = new THREE.Points(nodeGeo, nodeMat);
       group.add(nodesMesh);
       
       // Lines between layers
       const linePos = [];
       const connections: {start: THREE.Vector3, end: THREE.Vector3}[] = [];
       for(let i=0; i<layers.length-1; i++) {
           const current = layerPositions[i];
           const next = layerPositions[i+1];
           for(let a=0; a<current.length; a++) {
               for(let b=0; b<next.length; b++) {
                   linePos.push(current[a].x, current[a].y, current[a].z);
                   linePos.push(next[b].x, next[b].y, next[b].z);
                   connections.push({start: current[a], end: next[b]});
               }
           }
       }
       const lineGeo = new THREE.BufferGeometry();
       lineGeo.setAttribute('position', new THREE.Float32BufferAttribute(linePos, 3));
       const lineMat = new THREE.LineBasicMaterial({ color: 0xffffff, transparent: true, opacity: 0.15, depthWrite: false });
       group.add(new THREE.LineSegments(lineGeo, lineMat));
       
       // Compute signals (electrons)
       const signalsCount = 5;
       const sigGeo = new THREE.BufferGeometry();
       const sigPos = new Float32Array(signalsCount * 3);
       sigGeo.setAttribute('position', new THREE.BufferAttribute(sigPos, 3));
       const sigMat = new THREE.PointsMaterial({ map: glowTex, size: 12, color: 0xffffff, transparent: true, opacity: 1, depthWrite: false, blending: THREE.AdditiveBlending });
       const signalsMesh = new THREE.Points(sigGeo, sigMat);
       group.add(signalsMesh);
       
       const signalsData = Array(signalsCount).fill(0).map(() => ({
           conn: connections[Math.floor(Math.random() * connections.length)],
           progress: Math.random()
       }));
       
       group.position.copy(center);
       // Orient them somewhat randomly but clearly 2D planes
       group.rotation.set(Math.random()*0.5, Math.random()*0.5, Math.random()*0.5);
       scene.add(group);
       
       sceneElements.push({
           group,
           center,
           updateFade: (camPos: THREE.Vector3) => {
               const dist = camPos.distanceTo(center);
               let fade = 0;
               if (dist < 2000) {
                   fade = 1.0 - Math.max(0, (dist - 400) / 1600);
               }
               nodeMat.opacity = 0.8 * fade;
               lineMat.opacity = 0.15 * fade;
               sigMat.opacity = 1.0 * fade;
           },
           update: (time: number) => {
               group.rotation.y += 0.005;
               
               // Animate compute signals along connections
               for(let i=0; i<signalsCount; i++) {
                   const sd = signalsData[i];
                   sd.progress += 0.02;
                   if(sd.progress >= 1) {
                       sd.progress = 0;
                       sd.conn = connections[Math.floor(Math.random() * connections.length)];
                   }
                   sigPos[i*3] = sd.conn.start.x + (sd.conn.end.x - sd.conn.start.x) * sd.progress;
                   sigPos[i*3+1] = sd.conn.start.y + (sd.conn.end.y - sd.conn.start.y) * sd.progress;
                   sigPos[i*3+2] = sd.conn.start.z + (sd.conn.end.z - sd.conn.start.z) * sd.progress;
               }
               signalsMesh.geometry.attributes.position.needsUpdate = true;
           }
       });
    }
    
    // Placed early on
    createStructuredNN(0.02, new THREE.Vector3(150, 100, 0));
    createStructuredNN(0.18, new THREE.Vector3(-200, -100, 0));
    createStructuredNN(0.3, new THREE.Vector3(100, 150, 0));
    createStructuredNN(0.5, new THREE.Vector3(-150, -50, 0));

    // ----------------------------------------------------
    // ZONE 2: NANOPARTICLE WORMS
    // ----------------------------------------------------
    function createWorm(progress: number, offset: THREE.Vector3) {
        const center = getPathPoint(progress).add(offset);
        const spineLength = 60;
        const particlesPerSegment = 10;
        const totalParticles = spineLength * particlesPerSegment;
        
        const geo = new THREE.BufferGeometry();
        const pos = new Float32Array(totalParticles * 3);
        geo.setAttribute('position', new THREE.BufferAttribute(pos, 3));
        
        const pBaseOpacity = 0.35;
        const mat = new THREE.PointsMaterial({ 
            map: glowTex, 
            size: 10, 
            color: 0xffffff, 
            transparent: true, 
            opacity: pBaseOpacity, 
            depthWrite: false, 
            blending: THREE.AdditiveBlending 
        });
        const pts = new THREE.Points(geo, mat);
        pts.position.copy(center);
        scene.add(pts);
        
        const timeOffset = Math.random() * 100;
        
        sceneElements.push({
           group: pts,
           center,
           updateFade: (camPos: THREE.Vector3) => {
               const dist = camPos.distanceTo(center);
               let fade = 0;
               if (dist < 2500) {
                   fade = 1.0 - Math.max(0, (dist - 600) / 1900);
               }
               mat.opacity = pBaseOpacity * fade;
           },
           update: (time: number) => {
               const positions = pts.geometry.attributes.position.array as Float32Array;
               const tt = time + timeOffset;
               
               for(let s=0; s<spineLength; s++) {
                   const st = tt - s * 0.03;
                   const cx = Math.sin(st * 1.2) * 350;
                   const cy = Math.cos(st * 0.9) * 250;
                   const cz = Math.sin(st * 0.5) * 400;
                   
                   for(let p=0; p<particlesPerSegment; p++) {
                       const idx = (s * particlesPerSegment + p) * 3;
                       const angle = (p / particlesPerSegment) * Math.PI * 2 + st * 3;
                       const radius = 25 + Math.sin(st * 5 + p) * 15; 
                       
                       positions[idx] = cx + Math.cos(angle) * radius;
                       positions[idx+1] = cy + Math.sin(angle) * radius;
                       positions[idx+2] = cz + Math.cos(angle * 0.5) * radius;
                   }
               }
               pts.geometry.attributes.position.needsUpdate = true;
           }
        });
    }
    
    // Placed sequentially along the path
    createWorm(0.1, new THREE.Vector3(-150, 200, 0));
    createWorm(0.35, new THREE.Vector3(250, -150, 0));
    createWorm(0.65, new THREE.Vector3(-250, 100, 0));

    // ----------------------------------------------------
    // ZONE 3: SPIRALING GALAXY (Singularity)
    // ----------------------------------------------------
    function createSingularity(progress: number, offset: THREE.Vector3) {
       const center = getPathPoint(progress).add(offset);
       const count = 5000;
       const geo = new THREE.BufferGeometry();
       const pos = new Float32Array(count * 3);
       
       const arms = 4;
       
       for(let i=0; i<count; i++) {
           // Archimedean spiral math
           const t = Math.random() * Math.PI * 4; 
           const radius = (t / (Math.PI * 4)) * 1200; 
           
           const armOffset = (i % arms) * (Math.PI * 2 / arms);
           // Add natural galactic noise
           const noiseX = (Math.random() - 0.5) * 150;
           const noiseZ = (Math.random() - 0.5) * 150;
           
           const x = Math.cos(t + armOffset) * radius + noiseX;
           // Flatten the Y axis heavily to form a disc with a slight bulge in the center
           const bulge = Math.max(0, 1 - (radius / 500)) * 200;
           const y = (Math.random() - 0.5) * (radius * 0.1 + bulge); 
           const z = Math.sin(t + armOffset) * radius + noiseZ;
           
           pos[i*3] = x;
           pos[i*3+1] = y;
           pos[i*3+2] = z;
       }
       geo.setAttribute('position', new THREE.BufferAttribute(pos, 3));
       
       const pBaseOpacity = 0.5;
       const pMat = new THREE.PointsMaterial({ 
           map: glowTex, 
           size: 8, 
           color: 0xffffff, 
           transparent: true, 
           opacity: pBaseOpacity, 
           depthWrite: false,
           blending: THREE.AdditiveBlending
       });
       
       const pts = new THREE.Points(geo, pMat);
       const group = new THREE.Group();
       group.add(pts);
       group.position.copy(center);
       
       // Tilt the galaxy for a majestic view
       group.rotation.x = Math.PI * 0.25;
       group.rotation.z = Math.PI * 0.1;
       
       scene.add(group);
       
       sceneElements.push({
           group,
           center,
           updateFade: (camPos: THREE.Vector3) => {
               const dist = camPos.distanceTo(center);
               let fade = 0;
               if (dist < 4000) {
                   fade = 1.0 - Math.max(0, (dist - 1000) / 3000);
               }
               pMat.opacity = pBaseOpacity * fade;
           },
           update: (time: number) => {
               // Slowly spin the entire galaxy
               group.rotation.y = time * -0.2; 
           }
       });
    }
    
    // Positioned at the very end of the portfolio journey
    createSingularity(0.96, new THREE.Vector3(0, -300, 0));

    // ----------------------------------------------------
    // FLOATING MATH SYMBOLS (Integrals, Sigma, etc.)
    // ----------------------------------------------------
    const mathSymbols = ["∫", "∑", "∂", "∇", "λ", "θ", "μ", "π", "∞"];
    mathSymbols.forEach((sym, index) => {
       const tex = createMathTexture(sym);
       const mat = new THREE.SpriteMaterial({ map: tex, transparent: true, opacity: 0.4, depthWrite: false, blending: THREE.AdditiveBlending });
       
       // Create multiple instances of each symbol
       for(let i=0; i<15; i++) {
           const sprite = new THREE.Sprite(mat);
           // Randomly place along the entire path
           const prog = Math.random();
           const pt = getPathPoint(prog);
           sprite.position.set(
               pt.x + (Math.random() - 0.5) * 2000,
               pt.y + (Math.random() - 0.5) * 2000,
               pt.z + (Math.random() - 0.5) * 1000
           );
           const scale = 20 + Math.random() * 30;
           sprite.scale.set(scale, scale, 1);
           scene.add(sprite);
           
           updatables.push((time: number) => {
               sprite.position.y += Math.sin(time + index + i) * 0.2;
           });
       }
    });

    // ----------------------------------------------------
    // AMBIENT DUST
    // ----------------------------------------------------
    const dustCount = 3000;
    const dustGeo = new THREE.BufferGeometry();
    const dustPos = new Float32Array(dustCount * 3);
    for(let i=0; i<dustCount*3; i+=3) {
       dustPos[i] = (Math.random() - 0.5) * 3500;
       dustPos[i+1] = (Math.random() - 0.5) * 3500;
       dustPos[i+2] = 1500 - Math.random() * 7000; 
    }
    dustGeo.setAttribute("position", new THREE.BufferAttribute(dustPos, 3));
    const dustMat = new THREE.PointsMaterial({
       map: circleTex,
       color: 0xffffff,
       size: 3,
       transparent: true,
       opacity: 0.2, // Made dust more visible
       depthWrite: false,
       blending: THREE.AdditiveBlending
    });
    const dustSystem = new THREE.Points(dustGeo, dustMat);
    scene.add(dustSystem);

    // ----------------------------------------------------
    // INTERACTIVITY & ANIMATION
    // ----------------------------------------------------
    let mouseX = 0;
    let mouseY = 0;
    let scrollPercent = 0;
    
    const calculateScroll = () => {
        const h = document.documentElement;
        const b = document.body;
        const st = 'scrollTop';
        const sh = 'scrollHeight';
        
        const scrollAmount = (h[st] || b[st]);
        const maxScroll = (h[sh] || b[sh]) - h.clientHeight;
        
        scrollPercent = maxScroll > 0 ? scrollAmount / maxScroll : 0;
    };

    document.addEventListener("scroll", calculateScroll);
    window.addEventListener("resize", calculateScroll);

    document.addEventListener("mousemove", (event) => {
      mouseX = (event.clientX - window.innerWidth / 2) * 0.4;
      mouseY = (event.clientY - window.innerHeight / 2) * 0.4;
    });

    let time = 0;
    
    // We need to keep track of where the camera is currently looking to smooth it
    const currentLookTarget = new THREE.Vector3(0, 0, 0);
    // Initialize look target ahead of start
    currentLookTarget.copy(getPathPoint(0.05));

    function animate() {
      requestAnimationFrame(animate);
      time += 0.015;

      // Update all scene elements (rotate/move) and apply proximity fading
      sceneElements.forEach(el => {
         el.update(time);
         el.updateFade(camera.position);
      });
      
      updatables.forEach(fn => fn(time));

      // Update Camera (Curvy Video Game Journey)
      
      // Calculate where we should be on the path
      const targetPos = getPathPoint(scrollPercent);
      // Add mouse parallax
      targetPos.x += mouseX;
      targetPos.y += -mouseY;
      
      // Smoothly move camera towards the target position
      camera.position.lerp(targetPos, 0.05);
      
      // Calculate where we should look (slightly ahead on the path)
      // This makes the camera bank and turn into the curves
      const lookAheadPercent = Math.min(1.0, scrollPercent + 0.08);
      const targetLookPos = getPathPoint(lookAheadPercent);
      
      // Smoothly interpolate the look target for buttery smooth turns
      currentLookTarget.lerp(targetLookPos, 0.05);
      camera.lookAt(currentLookTarget);

      renderer.render(scene, camera);
    }

    window.addEventListener("resize", () => {
      camera.aspect = window.innerWidth / window.innerHeight;
      camera.updateProjectionMatrix();
      renderer.setSize(window.innerWidth, window.innerHeight);
    });
    
    calculateScroll();
    animate();
  }
</script>

<style>
  #background {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -1;
    background: transparent;
    pointer-events: none;
  }
</style>
